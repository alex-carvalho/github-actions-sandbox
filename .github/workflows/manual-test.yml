name: Manual Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Run simple test
        run: |
          echo "GitHub Actions workflow triggered manually!"
          echo "Current date and time:"
          date
          echo "Current directory:"
          pwd
